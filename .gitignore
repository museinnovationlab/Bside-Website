# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Secrets — never commit (this is a static site; nothing here should hold keys)
.env
.env.*
*.pem
*.p8
*-key.json
service_account*.json

# Build / deploy artifacts (if you later add a host)
.firebase/
.vercel/
node_modules/
dist/
